<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mypageMapper">
	
	<!-- <select id="selectAllMyBoast" resultType="boastResultMap">
		SELECT * FROM BOAST WHERE MEMBER_NO = #{memberNo}
	</select>
	
	<select id="selectAllMyClinic" resultType="clinicResultMap">
		SELECT * FROM CLINIC WHERE MEMBER_NO = #{memberNo}
	</select> -->
	
	<!-- <select id="selectAllMyCuriosity" resultType="curiosityResultMap">
		SELECT * FROM CURIOSITY WHERE MEMBER_NO = #{memberNo}
	</select> -->
</mapper>

